<div class="container">
    <div class="row">
        <div class="movie-card">
            <img src="{{poster}}" class="movie-img" alt="...">
        </div>
        <h1 class="text-white py-4">{{movie_title}}</h1>
        <h4 class="text-white">{{description}}</h4>
        <button>Write a review</button>
    </div>
    <div class="row">
        <h1>Review</h1>
        <div>
            
        </div>
    </div>
</div>