<!-- Loop over each object in the projects array -->

<div class="container">
  <div class="row row-cols-4">
    {{#each movies as |movie| }}
    <img src="{{movie.poster}}" class="img-fluid py-3" alt="...">
    {{/each}}
  </div>
</div>